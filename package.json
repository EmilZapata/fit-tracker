{
  "name": "fit-tracker-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:mobile": "pnpm --filter fit-tracker dev",
    "dev:studio": "turbo run dev --filter=sanity-studio",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.24.0"
}
